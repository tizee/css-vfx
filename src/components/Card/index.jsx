import styled from 'styled-components';

const Card = styled.div`
    position: relative;
    height: 200px;
    width: 200px;
    border-radius: 18px;
    border: 2px solid #fff;
    background: #fff;

    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
`;

export default Card;
